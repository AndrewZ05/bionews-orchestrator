<!-- AUTO-GENERATED by scripts/generate_schema_contract.py - DO NOT EDIT BY HAND -->
<!-- Re-generate: python scripts/generate_schema_contract.py -->

# Profile Database Schema Contract

**Version:** `v6.7`
**Datasets:** `bi-data-391216.profile_data`, `bi-data-391216.profile_data_candidate`, `bi-data-391216.profile_ops`, `bi-data-391216.profile_staging`
**Effective shape:** `16` consumer/reference tables + `9` ops tables + `6` staging helpers + `23` views
**Source of truth:** `shared/profile_database_manifest.py`, [sql/profile_database_ddl.sql](../sql/profile_database_ddl.sql), [sql/profile_database_views.sql](../sql/profile_database_views.sql), and active SQL `WRITES:` headers.

---

## Contract Levels

1. `Consumer core`
Stable tables and views that downstream users should understand and query.
2. `Derived signals`
Useful warehouse tables, but not the first consumer mental model.
3. `Reference`
Dictionaries and lookup/config support.
4. `Internal runtime`
Build logs, snapshots, restore diagnostics, and staging artifacts.

## Consumer Core

| Table | Purpose |
|-------|---------|
| `profile_core` | Single row per bn_id; persona, condition, demographic, HCP, and actionability fields. |
| `profile_identifiers` | All known identifiers per profile; the main cross-system join surface. |
| `profile_engagement` | Rolled-up behavioral and lifecycle summary per profile. |
| `profile_preferences` | Launch-era consolidated preferences surface: newsletter subscriptions plus forum settings. |
| `profile_survey_data` | Normalized LimeSurvey answers linked to bn_id. |
| `site_events` | GA4-backed site interaction fact table feeding the unified event surface. |
| `profile_zero_party` | Identity-linked poll, quiz, and other zero-party responses feeding the unified event surface. |

## Derived Signals

| Table | Purpose |
|-------|---------|
| `profile_content_affinity` | Per-condition browsing affinity derived from content consumption. |
| `profile_ad_attribution` | Per-click attribution facts and ad-touch history. |
| `profile_segment_tags` | Governed tags used for segmentation and promoted downstream signals. |

## Reference

| Table | Purpose |
|-------|---------|
| `conditions_dict` | Canonical condition vocabulary. |
| `symptoms_dict` | Canonical symptom vocabulary. |
| `treatments_dict` | Canonical treatment vocabulary. |
| `subtypes_dict` | Condition subtype vocabulary. |
| `dictionary_meta` | Dictionary provenance and curation metadata. |
| `profile_lookup` | General lookup/config table used by profile SQL modules. |

## Internal Runtime Tables (`profile_ops`)

| Table | Purpose |
|-------|---------|
| `profile_build_runs` | Run-level build observability. |
| `profile_build_steps` | Step-level build observability. |
| `profile_core_snapshot` | Point-in-time rebuild snapshot of profile_core. |
| `profile_field_changes` | Narrow audit log for the 8 persona-classification fields tracked in v6.5: condition_subtype, diagnosis_stage, preferred_condition, and the 5 role flags (is_patient, is_hcp, is_caregiver, is_family_or_friend, is_other). Not a generic field-level lineage surface - other field changes are not tracked here. |
| `profile_restore_unmapped` | Restore diagnostics for snapshot rows that could not be remapped. |
| `profile_publish_manifest` | Per-table publish telemetry for candidate-to-production promotion during rebuilds (copy status, verification counts, and errors). |
| `profile_dataset_leases` | Mutual-exclusion lease rows for rebuild publish (candidate dataset); managed by shared/profile_rebuild_hardening.py. |
| `profile_evictions` | Audit of bn_ids removed from profile_core by reconcile: merge-forward survivors, ineligible profiles, and rows whose xref disappeared. One row per eviction with the reason and the surviving bn_id where there is one. |

## Staging Helpers (`profile_staging`)

| Table | Purpose |
|-------|---------|
| `profile_core_app_snapshot` | Pre-rebuild snapshot of app-written fields used for restore. |
| `refresh_scope_bn_ids` | Changed-profile subset table used to scope refresh-safe writes. |
| `zero_party_staging` | Anonymous zero-party response staging before identity promotion. |
| `identity_xref_snapshot` | Build-local snapshot copy of identity_hub_data.bn_id_xref used to pin one consistent identity source image per run. |
| `identity_hub_snapshot` | Build-local snapshot copy of identity_hub_data.bn_id_hub used to pin acceptor/web activity reads per run. |
| `identity_persistence_snapshot` | Build-local snapshot copy of identity_hub_data.bn_id_persistence used to pin merge/split history reads per run. |

## Supported Views

| View | Support level | Purpose |
|------|---------------|---------|
| `profile_current_safe` | Default | Default query surface. One row per profile with sensitive fields redacted. |
| `profile_current` | Supported | Full sensitive surface for approved use only. |
| `profile_signals` | Supported | Unified one-row-per-profile derived signal summary over affinity, tags, and attribution. |
| `profile_explain` | Supported | One-row explainability surface for source, confidence, and supporting signals. |
| `profile_events` | Supported | Unified event timeline view over site_events and profile_zero_party. |
| `profile_contactability` | Supported | One-row-per-profile consent and reachability summary. |
| `profile_build_performance` | Supported | Step-level build and refresh performance telemetry over profile_ops logs. |
| `profile_release_status` | Supported | One-row current release / publish health summary over build runs and publish steps. |
| `profile_marketing_audience` | Supported | Marketing-safe audience view derived from profile_current_safe. |
| `profile_analytics_audience` | Supported | Aggregate-safe analytics surface with identifiers removed. |
| `profile_ops_audience` | Supported | Support and operations lookup surface. |
| `profile_exceptions` | Supported | Data quality conflict surface. |
| `profile_coverage` | Supported | Field fill-rate monitoring surface. |
| `profile_audience_hcp` | Supported | HCP targeting audience. |
| `profile_audience_patients_confirmed` | Supported | Confirmed patient audience. |
| `profile_audience_caregivers` | Supported | Caregiver audience. |
| `profile_audience_high_engagement` | Supported | High-engagement audience. |
| `profile_metrics` | Supported | Single definition of every headline audience metric, one row per bn_id. Booleans for known person, verified HCP, mailable, active email (bot-filtered), known/active member (participation) and logged-in (presence), plus dimensions to slice by. Consumers group this view rather than redefining metrics; the traps (tier, raw opens, cookie dates, NULL role flags) are solved inside it. |

## Compatibility Views

| View | Purpose |
|------|---------|
| `profile_engagement_monthly` | Compatibility view replacing the retired monthly engagement table. |
| `profile_newsletter_preferences` | Compatibility view over profile_preferences.newsletter_preferences. |
| `profile_forum_settings` | Compatibility view over profile_preferences.forum_settings. |
| `profile_consent` | COMPATIBILITY ONLY -- not the canonical consent fact. Re-shapes existing communication_opt_in / tracking_consent / GA4 OneTrust flags into a row-per-purpose surface for legacy consumers. Does not carry per-grant records or DSR state. For activation eligibility use profile_contactability; do not build marketing enforcement, compliance reporting, or legal decisions on this view. |

## Active Write Paths

| Step | SQL file | Primary writes |
|------|----------|----------------|
| `snapshot` | `sql/profile_database_snapshot_app_fields.sql` | profile_staging.profile_core_app_snapshot |
| `rebuild_scope` | `sql/profile_database_rebuild_scope_all.sql` | profile_staging.refresh_scope_bn_ids |
| `ddl` | `sql/profile_database_ddl.sql` | DDL only |
| `maintenance` | `sql/profile_database_maintenance.sql` | profile_data.profile_lookup, profile_data.conditions_dict, profile_data.symptoms_dict, profile_data.treatments_dict, profile_data.subtypes_dict, profile_data.dictionary_meta, profile_data.profile_engagement, profile_data.profile_core |
| `populate_identity_core` | `sql/populate_identity_core.sql` | profile_data.profile_identifiers (all cols), profile_data.profile_core (all cols), profile_ops.profile_field_changes (5 v6.5 role flags + preferred_condition genesis) |
| `populate_engagement` | `sql/populate_engagement.sql` | profile_data.profile_engagement (all cols), profile_data.profile_core, profile_data.profile_identifiers |
| `populate_survey` | `sql/populate_survey.sql` | profile_data.profile_survey_data (all cols), profile_data.profile_identifiers |
| `populate_newsletter_forum` | `sql/populate_newsletter_forum.sql` | profile_data.profile_preferences (all cols), profile_data.profile_core, profile_data.profile_identifiers |
| `populate_finalize` | `sql/populate_finalize.sql` | profile_data.profile_core (last_active_at, profile_updated_at), profile_data.profile_engagement |
| `fill_gaps_condition_site` | `sql/fill_gaps_condition_site.sql` | profile_data.profile_core (preferred_condition, site_domain) |
| `fill_gaps_site_domain` | `sql/fill_gaps_site_domain.sql` | profile_data.profile_core |
| `fill_gaps_ga4_engagement` | `sql/fill_gaps_ga4_engagement.sql` | profile_data.profile_engagement (ga4 aggs, forms, survey, tier) |
| `fill_gaps_aim_attribution` | `sql/fill_gaps_aim_attribution.sql` | profile_data.profile_core, profile_data.profile_ad_attribution, profile_data.profile_content_affinity |
| `fill_gaps_forum_infer` | `sql/fill_gaps_forum_infer.sql` | profile_data.profile_engagement, profile_data.profile_core |
| `enrich_condition_inference` | `sql/enrich_condition_inference.sql` | profile_data.profile_core (preferred_condition + lineage triple), profile_ops.profile_field_changes (lineage) |
| `backfill_site_events` | `sql/backfill_site_events_ga4.sql` | Unknown / inspect SQL manually |
| `enrich_subtypes_diagnosis` | `sql/enrich_subtypes_diagnosis.sql` | profile_data.profile_core (condition_subtype, condition_subtype_source, diagnosis_stage, diagnosis_stage_source) |
| `enrich_dictionaries_zeroparty` | `sql/enrich_dictionaries_zeroparty.sql` | profile_data.conditions_dict, profile_staging.zero_party_staging, profile_data.profile_zero_party |
| `enrich_segments_signup` | `sql/enrich_segments_signup.sql` | profile_data.profile_segment_tags, profile_data.profile_core (signup_source) |
| `enrich_completeness_limesurvey` | `sql/enrich_completeness_limesurvey.sql` | profile_data.profile_engagement, profile_data.profile_core |
| `enrich_hcp_buddypress` | `sql/enrich_hcp_buddypress.sql` | profile_data.profile_core |
| `enrich_mailchimp_ga4` | `sql/enrich_mailchimp_ga4.sql` | profile_data.profile_core, profile_data.profile_engagement |
| `enrich_condition_interest` | `sql/enrich_condition_interest.sql` | profile_data.profile_core (preferred_condition, interest_tags, follow_conditions, demographics) |
| `enrich_surveyengine` | `sql/enrich_surveyengine.sql` | profile_data.profile_core |
| `enrich_condition_normalization` | `sql/enrich_condition_normalization.sql` | profile_data.profile_core (preferred_condition_normalized + _source + _confidence + _updated_at), profile_ops.profile_field_changes (lineage) |
| `enrich_newsletter_clinical` | `sql/enrich_newsletter_clinical.sql` | profile_data.profile_core (joined_newsletter_topics, clinical_trials_interest, patient_community_interest) |
| `personas_classify` | `sql/personas_classify.sql` | profile_data.profile_core |
| `personas_patient` | `sql/personas_patient.sql` | profile_data.profile_core (symptom_tags, treatments_current, diagnosis_timing_band, diagnosis_stage, joined_patient_community), profile_ops.profile_field_changes (diagnosis_stage lineage) |
| `personas_caregiver` | `sql/personas_caregiver.sql` | profile_data.profile_core (caregiver_condition, caregiver_relationship, joined_caregiver_community, caregiver_focus_areas) |
| `personas_stage` | `sql/personas_stage.sql` | profile_data.profile_core (profile_stage) |
| `restore` | `sql/profile_database_restore_app_fields.sql` | profile_data.profile_core (app-authored cols), profile_ops.profile_restore_unmapped (diagnostic rows for unresolvable snapshots) |
| `views` | `sql/profile_database_views.sql` | profile_data views only |
| `snapshot_core` | `sql/profile_database_snapshot_core.sql` | profile_ops.profile_core_snapshot |
| `refresh_scope` | `sql/profile_database_refresh_scope.sql` | profile_staging.refresh_scope_bn_ids |
| `refresh` | `sql/profile_database_refresh.sql` | profile_data.profile_identifiers, profile_data.profile_core, profile_data.profile_engagement, profile_data.profile_survey_data, profile_data.site_events, profile_data.profile_zero_party, profile_ops.profile_field_changes |
| `reconcile` | `sql/profile_database_reconcile.sql` | Unknown / inspect SQL manually |
| `enrich_source_created_at` | `sql/enrich_source_created_at.sql` | Unknown / inspect SQL manually |

## Column Rules

| Column | Rule |
|--------|------|
| `profile_updated_at` | Use on `profile_core` |
| `loaded_at` | Use on satellites, staging, and compatibility surfaces |
| `created_at` | Use for row creation timestamps where applicable |

## Process Notes

1. `rebuild` stages physical consumer/reference tables in `profile_data_candidate` and only publishes after gates pass.
2. `rebuild` and `refresh` now pin identity reads by copying `bn_id_xref`, `bn_id_hub`, and `bn_id_persistence` into `profile_staging` before SQL steps begin.
3. `refresh` is guarded for oversized scopes and short-circuits when the scope is empty.
4. `profile_consent` remains a compatibility surface, not the canonical consent fact model.
5. Legacy monolithic SQL files under `sql/legacy/profile_database/` are not the active build path.
